node_modules
dist
.env
server/cache/*.json
*.local

# Raw vFairs export contains attendee names/emails — never commit it.
# (Root-level only: public/data/ holds the PII-stripped published snapshot.)
/data/
